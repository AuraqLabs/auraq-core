Layers via data-parallax-layer="0.0–1.0" multiplier
Mode: data-parallax="mouse" / data-parallax="scroll" / data-parallax="auto" (auto = mouse on desktop, scroll on touch — the default)
Easing: movement lags behind input, floaty feel
Boundary: unclamped, layers can exceed container edges
Asset sizing: your responsibility at the design/prep stage
